<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Parkrun.MVVM.Views.ChartPage"
             xmlns:microcharts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             Title="">

    <VerticalStackLayout>
        <Grid ColumnDefinitions="*,*">
            <Button
                Text="Ansicht verändern"
                Command="{Binding ToggleViewModus}"
                />
            
        </Grid>
        
        <ScrollView Orientation="Horizontal">
            <microcharts:ChartView Chart="{Binding LineChart}" HeightRequest="300" WidthRequest="{Binding ChartWidth}" Margin="0,10,0,0"/>
        </ScrollView>
    </VerticalStackLayout>
    
    
    

</ContentPage>